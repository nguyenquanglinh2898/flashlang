name: flash_lang
description: "FlashLang - hoc tu vung tieng Anh bang flashcard va push notification."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path: ^1.9.0
  flutter_local_notifications: ^17.0.0
  android_alarm_manager_plus: ^5.0.0
  workmanager: ^0.6.0
  flutter_tts: ^4.0.0
  image_picker: ^1.0.0
  file_picker: ^8.0.0
  csv: ^6.0.0
  share_plus: ^7.0.0
  provider: ^6.0.0
  path_provider: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
